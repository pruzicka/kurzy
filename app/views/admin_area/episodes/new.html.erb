<div class="flex items-end justify-between gap-4">
  <div class="flex flex-col gap-1">
    <h1 class="text-3xl font-semibold tracking-tight text-gray-900">Nová epizoda</h1>
    <p class="text-sm text-gray-600"><%= @subscription_plan.name %></p>
  </div>
</div>

<div class="mt-6 rounded-2xl border border-gray-200 bg-white p-6 shadow-sm">
  <%= render "form", episode: @episode, subscription_plan: @subscription_plan %>
</div>
