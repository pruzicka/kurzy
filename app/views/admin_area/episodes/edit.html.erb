<div class="flex items-end justify-between gap-4">
  <div class="flex flex-col gap-1">
    <h1 class="text-3xl font-semibold tracking-tight text-gray-900">Upravit epizodu</h1>
    <p class="text-sm text-gray-600"><%= @subscription_plan.name %> / <%= @episode.title %></p>
  </div>
  <%= link_to "Zpět na plán", admin_subscription_plan_path(@subscription_plan), class: "inline-flex items-center justify-center rounded-2xl border border-gray-300 bg-white px-4 py-2.5 text-sm font-medium text-gray-900 hover:bg-gray-50" %>
</div>

<div class="mt-6 rounded-2xl border border-gray-200 bg-white p-6 shadow-sm">
  <%= render "form", episode: @episode, subscription_plan: @subscription_plan %>
</div>
