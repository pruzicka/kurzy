<div class="flex flex-col gap-6">
  <div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between">
    <div class="flex flex-col gap-1">
      <h1 class="text-3xl font-semibold tracking-tight text-gray-900"><%= @course.name %></h1>
      <div class="text-sm text-gray-600">
        <span class="font-medium text-gray-900"><%= @course.status %></span>
        <span class="mx-2 text-gray-300">/</span>
        <%= number_to_currency(@course.price, unit: @course.currency, precision: 0) %>
      </div>
    </div>
    <div class="flex flex-wrap gap-2">
      <%= link_to "Upravit", edit_admin_course_path(@course), class: "rounded-2xl border border-gray-300 bg-white px-4 py-2.5 text-sm font-medium text-gray-900 hover:bg-gray-50" %>
      <%= link_to "Zpet na seznam", admin_courses_path, class: "rounded-2xl border border-gray-300 bg-white px-4 py-2.5 text-sm font-medium text-gray-900 hover:bg-gray-50" %>
    </div>
  </div>

  <div class="grid grid-cols-1 gap-6 lg:grid-cols-3">
    <div class="rounded-2xl border border-gray-200 bg-white p-6 shadow-sm lg:col-span-1">
      <h2 class="text-lg font-semibold tracking-tight text-gray-900">Popis</h2>
      <div class="mt-3 text-sm text-gray-700">
        <% if @course.description&.body&.present? %>
          <%= @course.description %>
        <% else %>
          <span class="text-gray-500">Bez popisu.</span>
        <% end %>
      </div>
    </div>

    <div class="rounded-2xl border border-gray-200 bg-white p-6 shadow-sm lg:col-span-2">
      <div class="flex items-center justify-between gap-4">
        <h2 class="text-lg font-semibold tracking-tight text-gray-900">Obsah</h2>
        <%= link_to "Nova kapitola", new_admin_course_chapter_path(@course), class: "rounded-2xl bg-gray-900 px-4 py-2.5 text-sm font-medium text-white hover:bg-black" %>
      </div>

      <% if @course.chapters.any? %>
        <div class="mt-5 space-y-4">
          <% @course.chapters.each do |chapter| %>
            <div class="rounded-2xl border border-gray-200 bg-gray-50 p-4">
              <div class="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between">
                <div class="flex flex-col gap-1">
                  <div class="flex items-center gap-2">
                    <span class="inline-flex rounded-full bg-white px-2.5 py-1 text-xs font-semibold text-gray-700 ring-1 ring-gray-200">#<%= chapter.position %></span>
                    <h3 class="text-base font-semibold tracking-tight text-gray-900"><%= chapter.title %></h3>
                    <% if chapter.is_mandatory? %>
                      <span class="inline-flex rounded-full border border-emerald-200 bg-emerald-50 px-2.5 py-1 text-xs font-medium text-emerald-900">mandatory</span>
                    <% end %>
                  </div>
                </div>

                <div class="flex flex-wrap gap-2">
                  <%= button_to "Nahoru", move_up_admin_course_chapter_path(@course, chapter), method: :patch, class: "rounded-xl border border-gray-300 bg-white px-3 py-2 text-sm font-medium text-gray-900 hover:bg-gray-50" %>
                  <%= button_to "Dolu", move_down_admin_course_chapter_path(@course, chapter), method: :patch, class: "rounded-xl border border-gray-300 bg-white px-3 py-2 text-sm font-medium text-gray-900 hover:bg-gray-50" %>
                  <%= link_to "Novy segment", new_admin_course_chapter_segment_path(@course, chapter), class: "rounded-xl bg-gray-900 px-3 py-2 text-sm font-medium text-white hover:bg-black" %>
                  <%= link_to "Upravit", edit_admin_course_chapter_path(@course, chapter), class: "rounded-xl border border-gray-300 bg-white px-3 py-2 text-sm font-medium text-gray-900 hover:bg-gray-50" %>
                  <%= button_to "Smazat", admin_course_chapter_path(@course, chapter), method: :delete, data: { turbo_confirm: "Smazat kapitolu i se segmenty?" }, class: "rounded-xl border border-rose-200 bg-rose-50 px-3 py-2 text-sm font-medium text-rose-900 hover:bg-rose-100" %>
                </div>
              </div>

              <% if chapter.segments.any? %>
                <div class="mt-4 overflow-hidden rounded-2xl border border-gray-200 bg-white">
                  <div class="divide-y divide-gray-100">
                    <% chapter.segments.each do |segment| %>
                      <div class="flex flex-col gap-3 px-4 py-3 sm:flex-row sm:items-center sm:justify-between">
                        <div class="flex items-center gap-2">
                          <span class="inline-flex rounded-full bg-gray-100 px-2.5 py-1 text-xs font-semibold text-gray-700">#<%= segment.position %></span>
                          <span class="text-sm font-medium text-gray-900"><%= segment.title %></span>
                        </div>
                        <div class="flex flex-wrap gap-2">
                          <%= button_to "Nahoru", move_up_admin_course_chapter_segment_path(@course, chapter, segment), method: :patch, class: "rounded-xl border border-gray-300 bg-white px-3 py-2 text-sm font-medium text-gray-900 hover:bg-gray-50" %>
                          <%= button_to "Dolu", move_down_admin_course_chapter_segment_path(@course, chapter, segment), method: :patch, class: "rounded-xl border border-gray-300 bg-white px-3 py-2 text-sm font-medium text-gray-900 hover:bg-gray-50" %>
                          <%= link_to "Upravit", edit_admin_course_chapter_segment_path(@course, chapter, segment), class: "rounded-xl border border-gray-300 bg-white px-3 py-2 text-sm font-medium text-gray-900 hover:bg-gray-50" %>
                          <%= button_to "Smazat", admin_course_chapter_segment_path(@course, chapter, segment), method: :delete, data: { turbo_confirm: "Smazat segment?" }, class: "rounded-xl border border-rose-200 bg-rose-50 px-3 py-2 text-sm font-medium text-rose-900 hover:bg-rose-100" %>
                        </div>
                      </div>
                    <% end %>
                  </div>
                </div>
              <% else %>
                <div class="mt-4 rounded-xl border border-dashed border-gray-300 bg-white p-4 text-sm text-gray-700">
                  Zatim zadne segmenty. Vytvor prvni.
                </div>
              <% end %>
            </div>
          <% end %>
        </div>
      <% else %>
        <div class="mt-5 rounded-2xl border border-dashed border-gray-300 bg-gray-50 p-8 text-sm text-gray-700">
          Zatim zadny obsah. Vytvor prvni kapitolu.
        </div>
      <% end %>
    </div>
  </div>
</div>
