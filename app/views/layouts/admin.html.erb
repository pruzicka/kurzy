<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Admin | Kurzy" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "lexxy", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "lexxy_content", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= Sentry.get_trace_propagation_meta.html_safe %>
  </head>

  <body class="bg-gradient-to-b from-gray-50 to-white">
    <div class="min-h-screen">
      <header class="sticky top-0 z-10 border-b border-gray-200 bg-white/80 backdrop-blur">
        <div class="container mx-auto flex items-center justify-between px-5 py-4">
          <div class="flex items-center gap-3">
            <span class="inline-flex h-9 w-9 items-center justify-center rounded-2xl bg-gray-900 text-sm font-semibold text-white">A</span>
            <div class="flex flex-col leading-tight">
              <span class="text-sm font-semibold tracking-tight text-gray-900">Admin</span>
              <span class="text-xs text-gray-600">Kurzy</span>
            </div>
          </div>

          <nav class="flex items-center gap-2 text-sm">
            <%= link_to "Dashboard", admin_root_path, class: "rounded-xl px-3 py-2 font-medium text-gray-900 hover:bg-gray-100" %>
            <%= link_to "Kurzy", admin_courses_path, class: "rounded-xl px-3 py-2 font-medium text-gray-900 hover:bg-gray-100" %>
            <%= link_to "Média", admin_media_assets_path, class: "rounded-xl px-3 py-2 font-medium text-gray-900 hover:bg-gray-100" %>
            <%= link_to "Objednávky", admin_orders_path, class: "rounded-xl px-3 py-2 font-medium text-gray-900 hover:bg-gray-100" %>
            <%= link_to "Uživatelé", admin_users_path, class: "rounded-xl px-3 py-2 font-medium text-gray-900 hover:bg-gray-100" %>
            <%= link_to "Slevy", admin_coupons_path, class: "rounded-xl px-3 py-2 font-medium text-gray-900 hover:bg-gray-100" %>
            <%= link_to "Štítky", admin_tags_path, class: "rounded-xl px-3 py-2 font-medium text-gray-900 hover:bg-gray-100" %>
            <%= link_to "Fakturace", admin_billing_companies_path, class: "rounded-xl px-3 py-2 font-medium text-gray-900 hover:bg-gray-100" %>
            <%= link_to "Web", root_path, class: "rounded-xl px-3 py-2 font-medium text-gray-900 hover:bg-gray-100" %>
            <details class="relative">
              <summary class="list-none">
                <div class="flex items-center gap-2 rounded-xl border border-gray-300 bg-white px-2.5 py-1.5 hover:bg-gray-50">
                  <span class="inline-flex h-9 w-9 items-center justify-center rounded-full bg-gray-900 text-sm font-semibold text-white">
                    <%= (current_admin.first_name.presence || current_admin.username.to_s.first || "A").to_s.first.upcase %>
                  </span>
                  <span class="hidden text-sm font-medium text-gray-900 sm:inline"><%= current_admin.username %></span>
                  <svg class="h-4 w-4 text-gray-400" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                    <path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 0 1 1.06.02L10 11.168l3.71-3.94a.75.75 0 1 1 1.08 1.04l-4.24 4.5a.75.75 0 0 1-1.08 0l-4.24-4.5a.75.75 0 0 1 .02-1.06Z" clip-rule="evenodd" />
                  </svg>
                </div>
              </summary>
              <div class="absolute right-0 mt-2 w-56 rounded-2xl border border-gray-200 bg-white p-2 shadow-lg">
                <div class="px-3 py-2 text-xs text-gray-500">Přihlášeno jako</div>
                <div class="px-3 pb-2 text-sm font-medium text-gray-900"><%= current_admin.username %></div>
                <div class="h-px bg-gray-100"></div>
                <%= link_to "Můj profil", edit_admin_profile_path, class: "mt-2 block rounded-xl px-3 py-2 text-sm font-medium text-gray-900 hover:bg-gray-50" %>
                <%= link_to "Jobs", good_job_path, target: "_blank", rel: "noopener", class: "block rounded-xl px-3 py-2 text-sm font-medium text-gray-900 hover:bg-gray-50" %>
                <%= button_to "Odhlásit", destroy_admin_session_path, method: :delete, class: "mt-1 w-full rounded-xl bg-gray-900 px-3 py-2 text-sm font-medium text-white hover:bg-black" %>
              </div>
            </details>
          </nav>
        </div>
      </header>

      <div class="container mx-auto px-5 py-8">
        <% if notice.present? %>
          <div class="mb-6 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-900"><%= notice %></div>
        <% end %>
        <% if alert.present? %>
          <div class="mb-6 rounded-xl border border-rose-200 bg-rose-50 px-4 py-3 text-sm text-rose-900"><%= alert %></div>
        <% end %>

        <%= yield %>
      </div>
    </div>
  </body>
</html>
