<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Kurzy" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Kurzy">
    <meta name="mobile-web-app-capable" content="yes">
    <% if content_for?(:meta_description) %>
      <meta name="description" content="<%= content_for(:meta_description) %>">
    <% end %>
    <% if content_for?(:canonical_url) %>
      <link rel="canonical" href="<%= content_for(:canonical_url) %>">
    <% end %>

    <meta property="og:site_name" content="Kurzy">
    <meta property="og:title" content="<%= content_for?(:og_title) ? content_for(:og_title) : (content_for?(:title) ? content_for(:title) : "Kurzy") %>">
    <% if content_for?(:og_description) %>
      <meta property="og:description" content="<%= content_for(:og_description) %>">
    <% end %>
    <% if content_for?(:og_image) %>
      <meta property="og:image" content="<%= content_for(:og_image) %>">
    <% end %>
    <meta property="og:type" content="<%= content_for?(:og_type) ? content_for(:og_type) : "website" %>">
    <meta property="og:locale" content="cs_CZ">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "lexxy", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "lexxy_content", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gradient-to-b from-gray-50 to-white">
    <% centered_layout = content_for?(:layout_centered) %>
    <% fullscreen_layout = content_for?(:layout_fullscreen) %>
    <div id="page_title" data-controller="page-title" class="hidden"><%= content_for(:title) || "Kurzy" %></div>
    <div class="<%= if centered_layout
                    "min-h-screen px-5 py-12 flex flex-col items-center justify-center gap-6"
                  elsif fullscreen_layout
                    "min-h-screen w-full flex flex-col"
                  else
                    "min-h-screen flex flex-col"
                  end %>">
      <% unless centered_layout || fullscreen_layout %>
        <%= render "shared/navbar" %>
      <% end %>
      <% if notice.present? %>
        <div class="<%= centered_layout ? "w-full max-w-md" : "container mx-auto mt-10 px-5 mb-6" %> rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-900"><%= notice %></div>
      <% end %>
      <% if alert.present? %>
        <div class="<%= centered_layout ? "w-full max-w-md" : "container mx-auto mt-10 px-5 mb-6" %> rounded-xl border border-rose-200 bg-rose-50 px-4 py-3 text-sm text-rose-900"><%= alert %></div>
      <% end %>

      <% if centered_layout %>
        <main class="w-full flex justify-center">
          <%= yield %>
        </main>
      <% elsif fullscreen_layout %>
        <main class="flex flex-col gap-6">
          <%= yield %>
        </main>
      <% else %>
        <main class="container mx-auto mt-10 px-5 flex flex-col gap-6">
          <%= yield %>
        </main>
      <% end %>

      <% unless centered_layout || fullscreen_layout %>
        <footer class="mt-auto w-full border-t border-gray-200 bg-white/80 backdrop-blur">
          <div class="<%= fullscreen_layout ? "mx-auto flex w-full max-w-6xl flex-col items-center justify-between gap-3 px-5 py-6 text-xs text-gray-600 sm:flex-row" : "container mx-auto flex flex-col items-center justify-between gap-3 px-5 py-6 text-xs text-gray-600 sm:flex-row" %>">
            <div class="text-gray-500">© <%= Time.current.year %> Kurzy</div>
            <div class="flex flex-wrap items-center gap-4">
              <%= link_to "Vyloučení odpovědnosti", disclaimer_path, class: "font-medium text-gray-700 hover:text-gray-900 hover:underline" %>
              <%= link_to "Všeobecné obchodní podmínky", terms_path, class: "font-medium text-gray-700 hover:text-gray-900 hover:underline" %>
              <%= link_to "Zpracování osobních údajů", privacy_path, class: "font-medium text-gray-700 hover:text-gray-900 hover:underline" %>
            </div>
          </div>
        </footer>
      <% end %>
    </div>
  </body>
</html>
